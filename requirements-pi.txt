# Core dependencies (lightweight - works on Raspberry Pi!)
opencv-python-headless
numpy
requests

# Pi Camera - MUST be installed via apt, not pip!
# Run
#
# If you're using a virtual environment, create it with --system-site-packages:
# python3 -m venv --system-site-packages .venv

# NOTE: ultralytics is NOT needed on Raspberry Pi!
# The ONNX detector uses only OpenCV DNN.
# 
# If you want to use the Ultralytics detector instead,
# set USE_ONNX_DETECTOR=false in config.py and install:
# pip install ultralytics torch torchvision --index-url https://www.piwheels.org/simple
